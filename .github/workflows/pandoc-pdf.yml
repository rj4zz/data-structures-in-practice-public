name: Generate PDF from Markdown and Create Release

on:
  workflow_dispatch:

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Generate timestamp tag
        id: tag
        run: echo "tag=snapshot-$(date +'%Y%m%d-%H%M%S')" >> $GITHUB_OUTPUT

      - name: Prepare LaTeX Triggers and File List
        id: files
        run: |
          # 1. Create the Roman numbering trigger
          echo '\pagenumbering{roman}' > start_roman.tex
          
          # 2. Create the TOC and Arabic numbering trigger
          echo '\newpage \tableofcontents \newpage \pagenumbering{arabic} \setcounter{page}{1}' > start_chapters.tex

          # 3. Create a text file containing the list of all Markdown files (one per line)
          # This avoids the "unexpected Tok" error and argument length limits.
          FM_REST=$(ls manuscript/front_matter/*.md 2>/dev/null | grep -vE "00_cover.md|03_table_of_contents.md" | sort)
          CH_FILES=$(ls manuscript/chapters/chapter*.md 2>/dev/null | sort)
          AP_FILES=$(ls manuscript/appendices/appendix_*.md 2>/dev/null | sort)
          
          printf "%s\n" $FM_REST $CH_FILES $AP_FILES LICENSE.md > file_list.txt

      - name: Convert Markdown to PDF
        uses: docker://pandoc/extra:latest
        with:
          args: >-
            --file-scope
            --include-before-body=start_roman.tex
            --include-before-body=start_chapters.tex
            -o data-structures-book.pdf
            --template=eisvogel
            --toc-depth=2
            --top-level-division=chapter
            --number-sections
            --highlight-style=pygments
            --pdf-engine=tectonic
            -V geometry:margin=1in
            -V linkcolor:blue
            -V book=true
            -V classoption=oneside
            -V title="Data Structures in Practice"
            -V subtitle="A Hardware-Aware Approach for System Software Engineers"
            -V author="Danny Jiang"
            -V titlepage=true
            $(cat file_list.txt)
